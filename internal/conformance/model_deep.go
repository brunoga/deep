// Code generated by deep-gen. DO NOT EDIT.
package conformance

import (
	"fmt"
	deep "github.com/brunoga/deep/v6"
	"github.com/brunoga/deep/v6/condition"
	gen "github.com/brunoga/deep/v6/gen"
	"log/slog"
	"reflect"
	"regexp"
	"strings"
)

// Patch applies p to t using the generated fast path.
func (t *Item) Patch(p deep.Patch[Item], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Item) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Item); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Item](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/id", "/ID":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.ID == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.ID, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.ID)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.ID = v
				return true, nil
			}
		}
	case "/label", "/Label":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Label == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Label, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Label)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Label = v
				return true, nil
			}
		}
	case "/count", "/Count":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Count == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Count, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Count)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Count = v
				return true, nil
			}
		}
	case "/done", "/Done":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(bool); ok {
				_match = t.Done == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Done, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Done)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[bool](op.New); ok {
				t.Done = v
				return true, nil
			}
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Item) Diff(other *Item) deep.Patch[Item] {
	p := deep.Patch[Item]{}
	if t.ID != other.ID {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/id", Old: t.ID, New: other.ID})
	}
	if t.Label != other.Label {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/label", Old: t.Label, New: other.Label})
	}
	if t.Count != other.Count {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/count", Old: t.Count, New: other.Count})
	}
	if t.Done != other.Done {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/done", Old: t.Done, New: other.Done})
	}

	return p
}

func (t *Item) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/id", "/ID":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.ID, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.ID))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field ID")
		}
		switch c.Op {
		case "==":
			return t.ID == _sv, nil
		case "!=":
			return t.ID != _sv, nil
		case ">":
			return t.ID > _sv, nil
		case "<":
			return t.ID < _sv, nil
		case ">=":
			return t.ID >= _sv, nil
		case "<=":
			return t.ID <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.ID == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.ID == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/label", "/Label":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Label, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Label))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Label")
		}
		switch c.Op {
		case "==":
			return t.Label == _sv, nil
		case "!=":
			return t.Label != _sv, nil
		case ">":
			return t.Label > _sv, nil
		case "<":
			return t.Label < _sv, nil
		case ">=":
			return t.Label >= _sv, nil
		case "<=":
			return t.Label <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Label == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Label == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/count", "/Count":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Count, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Count))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Count")
		}
		_fv := float64(t.Count)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Count == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Count == iv {
							return true, nil
						}
					case float64:
						if float64(t.Count) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/done", "/Done":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Done, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Done))
		}
		_bv, _ok := c.Value.(bool)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Done")
		}
		switch c.Op {
		case "==":
			return t.Done == _bv, nil
		case "!=":
			return t.Done != _bv, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Item) Equal(other *Item) bool {
	if t.ID != other.ID {
		return false
	}
	if t.Label != other.Label {
		return false
	}
	if t.Count != other.Count {
		return false
	}
	if t.Done != other.Done {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Item) Clone() *Item {
	res := &Item{
		ID:    t.ID,
		Label: t.Label,
		Count: t.Count,
		Done:  t.Done,
	}
	return res
}

// Patch applies p to t using the generated fast path.
func (t *Meta) Patch(p deep.Patch[Meta], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Meta) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Meta); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Meta](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/owner", "/Owner":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Owner == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Owner, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Owner)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Owner = v
				return true, nil
			}
		}
	case "/level", "/Level":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Level == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Level, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Level)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Level = v
				return true, nil
			}
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Meta) Diff(other *Meta) deep.Patch[Meta] {
	p := deep.Patch[Meta]{}
	if t.Owner != other.Owner {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/owner", Old: t.Owner, New: other.Owner})
	}
	if t.Level != other.Level {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/level", Old: t.Level, New: other.Level})
	}

	return p
}

func (t *Meta) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/owner", "/Owner":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Owner, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Owner))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Owner")
		}
		switch c.Op {
		case "==":
			return t.Owner == _sv, nil
		case "!=":
			return t.Owner != _sv, nil
		case ">":
			return t.Owner > _sv, nil
		case "<":
			return t.Owner < _sv, nil
		case ">=":
			return t.Owner >= _sv, nil
		case "<=":
			return t.Owner <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Owner == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Owner == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/level", "/Level":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Level, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Level))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Level")
		}
		_fv := float64(t.Level)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Level == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Level == iv {
							return true, nil
						}
					case float64:
						if float64(t.Level) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Meta) Equal(other *Meta) bool {
	if t.Owner != other.Owner {
		return false
	}
	if t.Level != other.Level {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Meta) Clone() *Meta {
	res := &Meta{
		Owner: t.Owner,
		Level: t.Level,
	}
	return res
}

// Patch applies p to t using the generated fast path.
func (t *Doc) Patch(p deep.Patch[Doc], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Doc) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Doc); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Doc](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/title", "/Title":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Title == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Title, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Title)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Title = v
				return true, nil
			}
		}
	case "/status", "/Status":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Status == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Status, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Status)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Status = v
				return true, nil
			}
		}
	case "/score", "/Score":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Score == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Score, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Score)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Score = v
				return true, nil
			}
		}
	case "/meta", "/Meta":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.(Meta); ok {
				_match = deep.Equal(t.Meta, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Meta, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Meta)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Meta](op.New); ok {
				t.Meta = v
				return true, nil
			}
		}
	case "/items", "/Items":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.([]Item); ok {
				_match = deep.Equal(t.Items, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Items, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Items)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[[]Item](op.New); ok {
				t.Items = v
				return true, nil
			}
		}
	case "/tags", "/Tags":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.([]string); ok {
				_match = deep.Equal(t.Tags, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Tags, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Tags)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[[]string](op.New); ok {
				t.Tags = v
				return true, nil
			}
		}
	case "/fields", "/Fields":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.(map[string]string); ok {
				_match = deep.Equal(t.Fields, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Fields, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Fields)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[map[string]string](op.New); ok {
				t.Fields = v
				return true, nil
			}
		}
	default:
		if strings.HasPrefix(op.Path, "/meta/") {
			op.Path = op.Path[len("/meta/")-1:]
			return (&t.Meta).applyOperation(op, logger)
		}
		if strings.HasPrefix(op.Path, "/fields/") {
			parts := strings.Split(op.Path[len("/fields/"):], "/")
			key := deep.UnescapePathKey(parts[0])
			if len(parts) == 1 && !op.Strict {
				if op.Kind == deep.OpRemove {
					delete(t.Fields, key)
					return true, nil
				}
				if v, ok := deep.ValueAs[string](op.New); ok && (op.Kind == deep.OpAdd || op.Kind == deep.OpReplace) {
					if t.Fields == nil {
						t.Fields = make(map[string]string)
					}
					t.Fields[key] = v
					return true, nil
				}
			}
		}
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Doc) Diff(other *Doc) deep.Patch[Doc] {
	p := deep.Patch[Doc]{}
	if t.Title != other.Title {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/title", Old: t.Title, New: other.Title})
	}
	if t.Status != other.Status {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/status", Old: t.Status, New: other.Status})
	}
	if t.Score != other.Score {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/score", Old: t.Score, New: other.Score})
	}
	subMeta := (&t.Meta).Diff(&other.Meta)
	for _, op := range subMeta.Operations {
		if op.Path == "" || op.Path == "/" {
			op.Path = "/meta"
		} else {
			op.Path = "/meta" + op.Path
		}
		p.Operations = append(p.Operations, op)
	}
	{
		if (t.Items == nil) != (other.Items == nil) {
			p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/items", Old: t.Items, New: other.Items})
		} else {
			otherByKey := make(map[any]int)
			for i, v := range other.Items {
				otherByKey[v.ID] = i
			}
			for _, v := range t.Items {
				if _, ok := otherByKey[v.ID]; !ok {
					p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpRemove, Path: "/items/" + deep.EscapePathKey(fmt.Sprintf("%v", v.ID)), Old: v})
				}
			}
			tByKey := make(map[any]int)
			for i, v := range t.Items {
				tByKey[v.ID] = i
			}
			for j := range other.Items {
				i, ok := tByKey[other.Items[j].ID]
				if !ok {
					p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpAdd, Path: "/items/" + deep.EscapePathKey(fmt.Sprintf("%v", other.Items[j].ID)), New: other.Items[j]})
					continue
				}
				prefix := "/items/" + deep.EscapePathKey(fmt.Sprintf("%v", other.Items[j].ID))
				sub := t.Items[i].Diff(&other.Items[j])
				for _, op := range sub.Operations {
					if op.Path == "" || op.Path == "/" {
						op.Path = prefix
					} else {
						op.Path = prefix + op.Path
					}
					p.Operations = append(p.Operations, op)
				}
			}
		}
	}
	if len(t.Tags) != len(other.Tags) || (t.Tags == nil) != (other.Tags == nil) {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/tags", Old: t.Tags, New: other.Tags})
	} else {
		for i := range t.Tags {
			if t.Tags[i] != other.Tags[i] {
				p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: fmt.Sprintf("/tags/%d", i), Old: t.Tags[i], New: other.Tags[i]})
			}
		}
	}
	{
		_mapFrom := len(p.Operations)
		if (t.Fields == nil) != (other.Fields == nil) {
			p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/fields", Old: t.Fields, New: other.Fields})
		} else {
			if other.Fields != nil {
				for k, v := range other.Fields {
					if t.Fields == nil {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/fields/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
						continue
					}
					if oldV, ok := t.Fields[k]; !ok || v != oldV {
						kind := deep.OpReplace
						if !ok {
							kind = deep.OpAdd
						}
						p.Operations = append(p.Operations, deep.Operation{Kind: kind, Path: "/fields/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), Old: oldV, New: v})
					}
				}
			}
			if t.Fields != nil {
				for k, v := range t.Fields {
					if _, ok := other.Fields[k]; !ok {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpRemove, Path: "/fields/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), Old: v})
					}
				}
			}
		}
		gen.SortOperations(p.Operations[_mapFrom:])
	}

	return p
}

func (t *Doc) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/title", "/Title":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Title, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Title))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Title")
		}
		switch c.Op {
		case "==":
			return t.Title == _sv, nil
		case "!=":
			return t.Title != _sv, nil
		case ">":
			return t.Title > _sv, nil
		case "<":
			return t.Title < _sv, nil
		case ">=":
			return t.Title >= _sv, nil
		case "<=":
			return t.Title <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Title == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Title == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/status", "/Status":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Status, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Status))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Status")
		}
		switch c.Op {
		case "==":
			return t.Status == _sv, nil
		case "!=":
			return t.Status != _sv, nil
		case ">":
			return t.Status > _sv, nil
		case "<":
			return t.Status < _sv, nil
		case ">=":
			return t.Status >= _sv, nil
		case "<=":
			return t.Status <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Status == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Status == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/score", "/Score":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Score, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Score))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Score")
		}
		_fv := float64(t.Score)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Score == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Score == iv {
							return true, nil
						}
					case float64:
						if float64(t.Score) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Doc) Equal(other *Doc) bool {
	if t.Title != other.Title {
		return false
	}
	if t.Status != other.Status {
		return false
	}
	if t.Score != other.Score {
		return false
	}
	if !(&t.Meta).Equal((&other.Meta)) {
		return false
	}
	if len(t.Items) != len(other.Items) {
		return false
	}
	for i := range t.Items {
		if !t.Items[i].Equal(&other.Items[i]) {
			return false
		}
	}
	if len(t.Tags) != len(other.Tags) {
		return false
	}
	for i := range t.Tags {
		if t.Tags[i] != other.Tags[i] {
			return false
		}
	}
	if len(t.Fields) != len(other.Fields) {
		return false
	}
	for k, v := range t.Fields {
		vOther, ok := other.Fields[k]
		if !ok {
			return false
		}
		if v != vOther {
			return false
		}
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Doc) Clone() *Doc {
	res := &Doc{
		Title:  t.Title,
		Status: t.Status,
		Score:  t.Score,
	}
	res.Meta = *(&t.Meta).Clone()
	if t.Items != nil {
		res.Items = make([]Item, len(t.Items))
		for i := range t.Items {
			res.Items[i] = *t.Items[i].Clone()
		}
	}
	if t.Tags != nil {
		res.Tags = make([]string, len(t.Tags))
		copy(res.Tags, t.Tags)
	}
	if t.Fields != nil {
		res.Fields = make(map[string]string)
		for k, v := range t.Fields {
			res.Fields[k] = v
		}
	}
	return res
}
