// Code generated by deep-gen. DO NOT EDIT.
package main

import (
	"fmt"
	deep "github.com/brunoga/deep/v5"
	"reflect"
	"regexp"
)

// ApplyOperation applies a single operation to Account efficiently.
func (t *Account) ApplyOperation(op deep.Operation) (bool, error) {
	if op.If != nil {
		ok, err := t.EvaluateCondition(*op.If)
		if err != nil || !ok {
			return true, err
		}
	}
	if op.Unless != nil {
		ok, err := t.EvaluateCondition(*op.Unless)
		if err == nil && ok {
			return true, nil
		}
	}

	if op.Path == "" || op.Path == "/" {
		if v, ok := op.New.(Account); ok {
			*t = v
			return true, nil
		}
		if m, ok := op.New.(map[string]any); ok {
			for k, v := range m {
				t.ApplyOperation(deep.Operation{Kind: op.Kind, Path: "/" + k, New: v})
			}
			return true, nil
		}
	}

	switch op.Path {
	case "/id", "/ID":
		if op.Kind == deep.OpLog {
			deep.Logger().Info("deep log", "message", op.New, "path", op.Path, "field", t.ID)
			return true, nil
		}
		if op.Kind == deep.OpReplace && op.Strict {
			if _oldV, ok := op.Old.(string); !ok || t.ID != _oldV {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.ID)
			}
		}
		if v, ok := op.New.(string); ok {
			t.ID = v
			return true, nil
		}
	case "/balance", "/Balance":
		if op.Kind == deep.OpLog {
			deep.Logger().Info("deep log", "message", op.New, "path", op.Path, "field", t.Balance)
			return true, nil
		}
		if op.Kind == deep.OpReplace && op.Strict {
			_oldOK := false
			if _oldV, ok := op.Old.(int); ok {
				_oldOK = t.Balance == _oldV
			}
			if !_oldOK {
				if _oldF, ok := op.Old.(float64); ok {
					_oldOK = float64(t.Balance) == _oldF
				}
			}
			if !_oldOK {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Balance)
			}
		}
		if v, ok := op.New.(int); ok {
			t.Balance = v
			return true, nil
		}
		if f, ok := op.New.(float64); ok {
			t.Balance = int(f)
			return true, nil
		}
	case "/status", "/Status":
		if op.Kind == deep.OpLog {
			deep.Logger().Info("deep log", "message", op.New, "path", op.Path, "field", t.Status)
			return true, nil
		}
		if op.Kind == deep.OpReplace && op.Strict {
			if _oldV, ok := op.Old.(string); !ok || t.Status != _oldV {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Status)
			}
		}
		if v, ok := op.New.(string); ok {
			t.Status = v
			return true, nil
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Account) Diff(other *Account) deep.Patch[Account] {
	p := deep.NewPatch[Account]()
	if t.ID != other.ID {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/id", Old: t.ID, New: other.ID})
	}
	if t.Balance != other.Balance {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/balance", Old: t.Balance, New: other.Balance})
	}
	if t.Status != other.Status {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/status", Old: t.Status, New: other.Status})
	}

	return p
}

func (t *Account) EvaluateCondition(c deep.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Apply {
			ok, err := t.EvaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Apply {
			ok, err := t.EvaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Apply) > 0 {
			ok, err := t.EvaluateCondition(*c.Apply[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/id", "/ID":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			return checkType(t.ID, c.Value.(string)), nil
		}
		if c.Op == "log" {
			deep.Logger().Info("deep condition log", "message", c.Value, "path", c.Path, "value", t.ID)
			return true, nil
		}
		if c.Op == "matches" {
			return regexp.MatchString(c.Value.(string), fmt.Sprintf("%v", t.ID))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field ID")
		}
		switch c.Op {
		case "==":
			return t.ID == _sv, nil
		case "!=":
			return t.ID != _sv, nil
		case ">":
			return t.ID > _sv, nil
		case "<":
			return t.ID < _sv, nil
		case ">=":
			return t.ID >= _sv, nil
		case "<=":
			return t.ID <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.ID == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.ID == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/balance", "/Balance":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			return checkType(t.Balance, c.Value.(string)), nil
		}
		if c.Op == "log" {
			deep.Logger().Info("deep condition log", "message", c.Value, "path", c.Path, "value", t.Balance)
			return true, nil
		}
		if c.Op == "matches" {
			return regexp.MatchString(c.Value.(string), fmt.Sprintf("%v", t.Balance))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Balance")
		}
		_fv := float64(t.Balance)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Balance == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Balance == iv {
							return true, nil
						}
					case float64:
						if float64(t.Balance) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/status", "/Status":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			return checkType(t.Status, c.Value.(string)), nil
		}
		if c.Op == "log" {
			deep.Logger().Info("deep condition log", "message", c.Value, "path", c.Path, "value", t.Status)
			return true, nil
		}
		if c.Op == "matches" {
			return regexp.MatchString(c.Value.(string), fmt.Sprintf("%v", t.Status))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Status")
		}
		switch c.Op {
		case "==":
			return t.Status == _sv, nil
		case "!=":
			return t.Status != _sv, nil
		case ">":
			return t.Status > _sv, nil
		case "<":
			return t.Status < _sv, nil
		case ">=":
			return t.Status >= _sv, nil
		case "<=":
			return t.Status <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Status == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Status == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	}
	return false, fmt.Errorf("unsupported condition path or op: %s", c.Path)
}

// Equal returns true if t and other are deeply equal.
func (t *Account) Equal(other *Account) bool {
	if t.ID != other.ID {
		return false
	}
	if t.Balance != other.Balance {
		return false
	}
	if t.Status != other.Status {
		return false
	}
	return true
}

// Copy returns a deep copy of t.
func (t *Account) Copy() *Account {
	res := &Account{
		ID:      t.ID,
		Balance: t.Balance,
		Status:  t.Status,
	}
	return res
}

func contains[M ~map[K]V, K comparable, V any](m M, k K) bool {
	_, ok := m[k]
	return ok
}

func checkType(v any, typeName string) bool {
	switch typeName {
	case "string":
		_, ok := v.(string)
		return ok
	case "number":
		switch v.(type) {
		case int, int8, int16, int32, int64, uint, uint8, uint16, uint32, uint64, float32, float64:
			return true
		}
	case "boolean":
		_, ok := v.(bool)
		return ok
	case "object":
		rv := reflect.ValueOf(v)
		return rv.Kind() == reflect.Struct || rv.Kind() == reflect.Map
	case "array":
		rv := reflect.ValueOf(v)
		return rv.Kind() == reflect.Slice || rv.Kind() == reflect.Array
	case "null":
		if v == nil {
			return true
		}
		rv := reflect.ValueOf(v)
		return (rv.Kind() == reflect.Pointer || rv.Kind() == reflect.Interface ||
			rv.Kind() == reflect.Slice || rv.Kind() == reflect.Map) && rv.IsNil()
	}
	return false
}
