// Code generated by deep-gen. DO NOT EDIT.
package main

import (
	"fmt"
	"github.com/brunoga/deep/v5"
)

// ApplyOperation applies a single operation to Account efficiently.
func (t *Account) ApplyOperation(op v5.Operation) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil || !ok {
			return true, err
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err == nil && ok {
			return true, nil
		}
	}

	if op.Path == "" || op.Path == "/" {
		if v, ok := op.New.(Account); ok {
			*t = v
			return true, nil
		}
		if m, ok := op.New.(map[string]any); ok {
			for k, v := range m {
				t.ApplyOperation(v5.Operation{Kind: op.Kind, Path: "/" + k, New: v})
			}
			return true, nil
		}
	}

	switch op.Path {
	case "/id", "/ID":
		if v, ok := op.New.(string); ok {
			t.ID = v
			return true, nil
		}
	case "/balance", "/Balance":
		if v, ok := op.New.(int); ok {
			t.Balance = v
			return true, nil
		}
		if f, ok := op.New.(float64); ok {
			t.Balance = int(f)
			return true, nil
		}
	case "/status", "/Status":
		if v, ok := op.New.(string); ok {
			t.Status = v
			return true, nil
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Account) Diff(other *Account) v5.Patch[Account] {
	p := v5.NewPatch[Account]()
	if t.ID != other.ID {
		p.Operations = append(p.Operations, v5.Operation{
			Kind: v5.OpReplace,
			Path: "/id",
			Old:  t.ID,
			New:  other.ID,
		})
	}
	if t.Balance != other.Balance {
		p.Operations = append(p.Operations, v5.Operation{
			Kind: v5.OpReplace,
			Path: "/balance",
			Old:  t.Balance,
			New:  other.Balance,
		})
	}
	if t.Status != other.Status {
		p.Operations = append(p.Operations, v5.Operation{
			Kind: v5.OpReplace,
			Path: "/status",
			Old:  t.Status,
			New:  other.Status,
		})
	}
	return p
}

func (t *Account) evaluateCondition(c v5.Condition) (bool, error) {
	switch c.Path {
	case "/id", "/ID":
		switch c.Op {
		case "==":
			return t.ID == c.Value.(string), nil
		case "!=":
			return t.ID != c.Value.(string), nil
		}
	case "/balance", "/Balance":
		switch c.Op {
		case "==":
			return t.Balance == c.Value.(int), nil
		case "!=":
			return t.Balance != c.Value.(int), nil
		}
	case "/status", "/Status":
		switch c.Op {
		case "==":
			return t.Status == c.Value.(string), nil
		case "!=":
			return t.Status != c.Value.(string), nil
		}
	}
	return false, fmt.Errorf("unsupported condition path or op: %s", c.Path)
}

// Equal returns true if t and other are deeply equal.
func (t *Account) Equal(other *Account) bool {
	if t.ID != other.ID {
		return false
	}
	if t.Balance != other.Balance {
		return false
	}
	if t.Status != other.Status {
		return false
	}
	return true
}

// Copy returns a deep copy of t.
func (t *Account) Copy() *Account {
	res := &Account{
		ID:      t.ID,
		Balance: t.Balance,
		Status:  t.Status,
	}
	return res
}

func contains[M ~map[K]V, K comparable, V any](m M, k K) bool {
	_, ok := m[k]
	return ok
}
