// Code generated by deep-gen. DO NOT EDIT.
package model

import (
	"fmt"
	deep "github.com/brunoga/deep/v6"
	"github.com/brunoga/deep/v6/condition"
	gen "github.com/brunoga/deep/v6/gen"
	"log/slog"
	"reflect"
	"regexp"
	"strings"
)

// Patch applies p to t using the generated fast path.
func (t *Reading) Patch(p deep.Patch[Reading], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Reading) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Reading); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Reading](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/value", "/Value":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(float64); ok {
				_match = t.Value == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Value, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Value)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[float64](op.New); ok {
				t.Value = v
				return true, nil
			}
		}
	case "/unit", "/Unit":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Unit == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Unit, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Unit)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Unit = v
				return true, nil
			}
		}
	case "/by", "/By":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.By == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.By, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.By)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.By = v
				return true, nil
			}
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Reading) Diff(other *Reading) deep.Patch[Reading] {
	p := deep.Patch[Reading]{}
	if t.Value != other.Value {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/value", Old: t.Value, New: other.Value})
	}
	if t.Unit != other.Unit {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/unit", Old: t.Unit, New: other.Unit})
	}
	if t.By != other.By {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/by", Old: t.By, New: other.By})
	}

	return p
}

func (t *Reading) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/value", "/Value":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Value, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Value))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case float64:
			_cv = float64(v)
		case int:
			_cv = float64(v)
		default:
			return false, fmt.Errorf("condition value type mismatch for field Value")
		}
		_fv := float64(t.Value)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []float64:
				for _, v := range vals {
					if t.Value == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case float64:
						if t.Value == iv {
							return true, nil
						}
					case int:
						if float64(t.Value) == float64(iv) {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/unit", "/Unit":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Unit, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Unit))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Unit")
		}
		switch c.Op {
		case "==":
			return t.Unit == _sv, nil
		case "!=":
			return t.Unit != _sv, nil
		case ">":
			return t.Unit > _sv, nil
		case "<":
			return t.Unit < _sv, nil
		case ">=":
			return t.Unit >= _sv, nil
		case "<=":
			return t.Unit <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Unit == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Unit == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/by", "/By":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.By, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.By))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field By")
		}
		switch c.Op {
		case "==":
			return t.By == _sv, nil
		case "!=":
			return t.By != _sv, nil
		case ">":
			return t.By > _sv, nil
		case "<":
			return t.By < _sv, nil
		case ">=":
			return t.By >= _sv, nil
		case "<=":
			return t.By <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.By == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.By == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Reading) Equal(other *Reading) bool {
	if t.Value != other.Value {
		return false
	}
	if t.Unit != other.Unit {
		return false
	}
	if t.By != other.By {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Reading) Clone() *Reading {
	res := &Reading{
		Value: t.Value,
		Unit:  t.Unit,
		By:    t.By,
	}
	return res
}

// Patch applies p to t using the generated fast path.
func (t *Check) Patch(p deep.Patch[Check], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Check) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Check); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Check](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/id", "/ID":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.ID == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.ID, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.ID)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.ID = v
				return true, nil
			}
		}
	case "/label", "/Label":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Label == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Label, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Label)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Label = v
				return true, nil
			}
		}
	case "/done", "/Done":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(bool); ok {
				_match = t.Done == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Done, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Done)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[bool](op.New); ok {
				t.Done = v
				return true, nil
			}
		}
	case "/by", "/By":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.By == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.By, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.By)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.By = v
				return true, nil
			}
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Check) Diff(other *Check) deep.Patch[Check] {
	p := deep.Patch[Check]{}
	if t.ID != other.ID {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/id", Old: t.ID, New: other.ID})
	}
	if t.Label != other.Label {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/label", Old: t.Label, New: other.Label})
	}
	if t.Done != other.Done {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/done", Old: t.Done, New: other.Done})
	}
	if t.By != other.By {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/by", Old: t.By, New: other.By})
	}

	return p
}

func (t *Check) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/id", "/ID":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.ID, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.ID))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field ID")
		}
		switch c.Op {
		case "==":
			return t.ID == _sv, nil
		case "!=":
			return t.ID != _sv, nil
		case ">":
			return t.ID > _sv, nil
		case "<":
			return t.ID < _sv, nil
		case ">=":
			return t.ID >= _sv, nil
		case "<=":
			return t.ID <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.ID == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.ID == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/label", "/Label":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Label, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Label))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Label")
		}
		switch c.Op {
		case "==":
			return t.Label == _sv, nil
		case "!=":
			return t.Label != _sv, nil
		case ">":
			return t.Label > _sv, nil
		case "<":
			return t.Label < _sv, nil
		case ">=":
			return t.Label >= _sv, nil
		case "<=":
			return t.Label <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Label == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Label == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/done", "/Done":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Done, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Done))
		}
		_bv, _ok := c.Value.(bool)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Done")
		}
		switch c.Op {
		case "==":
			return t.Done == _bv, nil
		case "!=":
			return t.Done != _bv, nil
		}
	case "/by", "/By":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.By, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.By))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field By")
		}
		switch c.Op {
		case "==":
			return t.By == _sv, nil
		case "!=":
			return t.By != _sv, nil
		case ">":
			return t.By > _sv, nil
		case "<":
			return t.By < _sv, nil
		case ">=":
			return t.By >= _sv, nil
		case "<=":
			return t.By <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.By == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.By == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Check) Equal(other *Check) bool {
	if t.ID != other.ID {
		return false
	}
	if t.Label != other.Label {
		return false
	}
	if t.Done != other.Done {
		return false
	}
	if t.By != other.By {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Check) Clone() *Check {
	res := &Check{
		ID:    t.ID,
		Label: t.Label,
		Done:  t.Done,
		By:    t.By,
	}
	return res
}

// Patch applies p to t using the generated fast path.
func (t *Asset) Patch(p deep.Patch[Asset], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Asset) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Asset); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Asset](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/id", "/ID":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.ID == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.ID, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.ID)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.ID = v
				return true, nil
			}
		}
	case "/name", "/Name":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Name == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Name, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Name)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Name = v
				return true, nil
			}
		}
	case "/site", "/Site":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Site == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Site, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Site)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Site = v
				return true, nil
			}
		}
	case "/status", "/Status":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(Status); ok {
				_match = t.Status == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Status, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Status)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Status](op.New); ok {
				t.Status = v
				return true, nil
			}
		}
	case "/assignee", "/Assignee":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Assignee == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Assignee, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Assignee)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Assignee = v
				return true, nil
			}
		}
	case "/readings", "/Readings":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.(map[string]Reading); ok {
				_match = deep.Equal(t.Readings, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Readings, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Readings)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[map[string]Reading](op.New); ok {
				t.Readings = v
				return true, nil
			}
		}
	case "/checks", "/Checks":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.([]Check); ok {
				_match = deep.Equal(t.Checks, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Checks, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Checks)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[[]Check](op.New); ok {
				t.Checks = v
				return true, nil
			}
		}
	case "/notes", "/Notes":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(string); ok {
				_match = t.Notes == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Notes, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Notes)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[string](op.New); ok {
				t.Notes = v
				return true, nil
			}
		}
	default:
		if strings.HasPrefix(op.Path, "/readings/") {
			parts := strings.Split(op.Path[len("/readings/"):], "/")
			key := deep.UnescapePathKey(parts[0])
			if len(parts) == 1 && !op.Strict {
				if op.Kind == deep.OpRemove {
					delete(t.Readings, key)
					return true, nil
				}
				if v, ok := deep.ValueAs[Reading](op.New); ok && (op.Kind == deep.OpAdd || op.Kind == deep.OpReplace) {
					if t.Readings == nil {
						t.Readings = make(map[string]Reading)
					}
					t.Readings[key] = v
					return true, nil
				}
			}
		}
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Asset) Diff(other *Asset) deep.Patch[Asset] {
	p := deep.Patch[Asset]{}
	if t.ID != other.ID {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/id", Old: t.ID, New: other.ID})
	}
	if t.Name != other.Name {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/name", Old: t.Name, New: other.Name})
	}
	if t.Site != other.Site {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/site", Old: t.Site, New: other.Site})
	}
	if t.Status != other.Status {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/status", Old: t.Status, New: other.Status})
	}
	if t.Assignee != other.Assignee {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/assignee", Old: t.Assignee, New: other.Assignee})
	}
	{
		_mapFrom := len(p.Operations)
		if (t.Readings == nil) != (other.Readings == nil) {
			p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/readings", Old: t.Readings, New: other.Readings})
		} else {
			if other.Readings != nil {
				for k, v := range other.Readings {
					if t.Readings == nil {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/readings/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
						continue
					}
					oldV, ok := t.Readings[k]
					switch {
					case !ok:
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpAdd, Path: "/readings/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
					case !oldV.Equal(&v):
						sub := oldV.Diff(&v)
						prefix := "/readings/" + deep.EscapePathKey(fmt.Sprintf("%v", k))
						for _, op := range sub.Operations {
							if op.Path == "" || op.Path == "/" {
								op.Path = prefix
							} else {
								op.Path = prefix + op.Path
							}
							p.Operations = append(p.Operations, op)
						}
					}
				}
			}
			if t.Readings != nil {
				for k, v := range t.Readings {
					if _, ok := other.Readings[k]; !ok {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpRemove, Path: "/readings/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), Old: v})
					}
				}
			}
		}
		gen.SortOperations(p.Operations[_mapFrom:])
	}
	{
		if (t.Checks == nil) != (other.Checks == nil) {
			p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/checks", Old: t.Checks, New: other.Checks})
		} else {
			otherByKey := make(map[any]int)
			for i, v := range other.Checks {
				otherByKey[v.ID] = i
			}
			for _, v := range t.Checks {
				if _, ok := otherByKey[v.ID]; !ok {
					p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpRemove, Path: "/checks/" + deep.EscapePathKey(fmt.Sprintf("%v", v.ID)), Old: v})
				}
			}
			tByKey := make(map[any]int)
			for i, v := range t.Checks {
				tByKey[v.ID] = i
			}
			for j := range other.Checks {
				i, ok := tByKey[other.Checks[j].ID]
				if !ok {
					p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpAdd, Path: "/checks/" + deep.EscapePathKey(fmt.Sprintf("%v", other.Checks[j].ID)), New: other.Checks[j]})
					continue
				}
				prefix := "/checks/" + deep.EscapePathKey(fmt.Sprintf("%v", other.Checks[j].ID))
				sub := t.Checks[i].Diff(&other.Checks[j])
				for _, op := range sub.Operations {
					if op.Path == "" || op.Path == "/" {
						op.Path = prefix
					} else {
						op.Path = prefix + op.Path
					}
					p.Operations = append(p.Operations, op)
				}
			}
		}
	}
	if t.Notes != other.Notes {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/notes", Old: t.Notes, New: other.Notes})
	}

	return p
}

func (t *Asset) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/id", "/ID":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.ID, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.ID))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field ID")
		}
		switch c.Op {
		case "==":
			return t.ID == _sv, nil
		case "!=":
			return t.ID != _sv, nil
		case ">":
			return t.ID > _sv, nil
		case "<":
			return t.ID < _sv, nil
		case ">=":
			return t.ID >= _sv, nil
		case "<=":
			return t.ID <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.ID == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.ID == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/name", "/Name":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Name, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Name))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Name")
		}
		switch c.Op {
		case "==":
			return t.Name == _sv, nil
		case "!=":
			return t.Name != _sv, nil
		case ">":
			return t.Name > _sv, nil
		case "<":
			return t.Name < _sv, nil
		case ">=":
			return t.Name >= _sv, nil
		case "<=":
			return t.Name <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Name == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Name == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/site", "/Site":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Site, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Site))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Site")
		}
		switch c.Op {
		case "==":
			return t.Site == _sv, nil
		case "!=":
			return t.Site != _sv, nil
		case ">":
			return t.Site > _sv, nil
		case "<":
			return t.Site < _sv, nil
		case ">=":
			return t.Site >= _sv, nil
		case "<=":
			return t.Site <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Site == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Site == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/status", "/Status":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Status, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Status))
		}
		switch c.Op {
		case "==":
			return fmt.Sprintf("%v", t.Status) == fmt.Sprintf("%v", c.Value), nil
		case "!=":
			return fmt.Sprintf("%v", t.Status) != fmt.Sprintf("%v", c.Value), nil
		}
	case "/assignee", "/Assignee":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Assignee, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Assignee))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Assignee")
		}
		switch c.Op {
		case "==":
			return t.Assignee == _sv, nil
		case "!=":
			return t.Assignee != _sv, nil
		case ">":
			return t.Assignee > _sv, nil
		case "<":
			return t.Assignee < _sv, nil
		case ">=":
			return t.Assignee >= _sv, nil
		case "<=":
			return t.Assignee <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Assignee == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Assignee == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	case "/notes", "/Notes":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Notes, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Notes))
		}
		_sv, _ok := c.Value.(string)
		if !_ok {
			return false, fmt.Errorf("condition value type mismatch for field Notes")
		}
		switch c.Op {
		case "==":
			return t.Notes == _sv, nil
		case "!=":
			return t.Notes != _sv, nil
		case ">":
			return t.Notes > _sv, nil
		case "<":
			return t.Notes < _sv, nil
		case ">=":
			return t.Notes >= _sv, nil
		case "<=":
			return t.Notes <= _sv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []string:
				for _, v := range vals {
					if t.Notes == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					if sv, ok := v.(string); ok && t.Notes == sv {
						return true, nil
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Asset) Equal(other *Asset) bool {
	if t.ID != other.ID {
		return false
	}
	if t.Name != other.Name {
		return false
	}
	if t.Site != other.Site {
		return false
	}
	if t.Status != other.Status {
		return false
	}
	if t.Assignee != other.Assignee {
		return false
	}
	if len(t.Readings) != len(other.Readings) {
		return false
	}
	for k, v := range t.Readings {
		vOther, ok := other.Readings[k]
		if !ok {
			return false
		}
		if !v.Equal(&vOther) {
			return false
		}
	}
	if len(t.Checks) != len(other.Checks) {
		return false
	}
	for i := range t.Checks {
		if !t.Checks[i].Equal(&other.Checks[i]) {
			return false
		}
	}
	if t.Notes != other.Notes {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Asset) Clone() *Asset {
	res := &Asset{
		ID:       t.ID,
		Name:     t.Name,
		Site:     t.Site,
		Status:   t.Status,
		Assignee: t.Assignee,
		Notes:    t.Notes,
	}
	if t.Readings != nil {
		res.Readings = make(map[string]Reading)
		for k, v := range t.Readings {
			res.Readings[k] = *v.Clone()
		}
	}
	if t.Checks != nil {
		res.Checks = make([]Check, len(t.Checks))
		for i := range t.Checks {
			res.Checks[i] = *t.Checks[i].Clone()
		}
	}
	return res
}
