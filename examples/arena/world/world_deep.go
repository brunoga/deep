// Code generated by deep-gen. DO NOT EDIT.
package world

import (
	"fmt"
	deep "github.com/brunoga/deep/v6"
	"github.com/brunoga/deep/v6/condition"
	gen "github.com/brunoga/deep/v6/gen"
	"log/slog"
	"reflect"
	"regexp"
	"strings"
)

// Patch applies p to t using the generated fast path.
func (t *World) Patch(p deep.Patch[World], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *World) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(World); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[World](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/tick", "/Tick":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int64); ok {
				_match = t.Tick == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Tick, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Tick)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int64](op.New); ok {
				t.Tick = v
				return true, nil
			}
		}
	case "/width", "/Width":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Width == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Width, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Width)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Width = v
				return true, nil
			}
		}
	case "/height", "/Height":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Height == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Height, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Height)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Height = v
				return true, nil
			}
		}
	case "/players", "/Players":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.(map[string]Player); ok {
				_match = deep.Equal(t.Players, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Players, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Players)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[map[string]Player](op.New); ok {
				t.Players = v
				return true, nil
			}
		}
	case "/gems", "/Gems":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if old, ok := op.Old.(map[string]Gem); ok {
				_match = deep.Equal(t.Gems, old)
			}
			if !_match {
				_match = deep.EqualCoerced(t.Gems, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Gems)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[map[string]Gem](op.New); ok {
				t.Gems = v
				return true, nil
			}
		}
	default:
		if strings.HasPrefix(op.Path, "/players/") {
			parts := strings.Split(op.Path[len("/players/"):], "/")
			key := deep.UnescapePathKey(parts[0])
			if len(parts) == 1 && !op.Strict {
				if op.Kind == deep.OpRemove {
					delete(t.Players, key)
					return true, nil
				}
				if v, ok := deep.ValueAs[Player](op.New); ok && (op.Kind == deep.OpAdd || op.Kind == deep.OpReplace) {
					if t.Players == nil {
						t.Players = make(map[string]Player)
					}
					t.Players[key] = v
					return true, nil
				}
			}
		}
		if strings.HasPrefix(op.Path, "/gems/") {
			parts := strings.Split(op.Path[len("/gems/"):], "/")
			key := deep.UnescapePathKey(parts[0])
			if len(parts) == 1 && !op.Strict {
				if op.Kind == deep.OpRemove {
					delete(t.Gems, key)
					return true, nil
				}
				if v, ok := deep.ValueAs[Gem](op.New); ok && (op.Kind == deep.OpAdd || op.Kind == deep.OpReplace) {
					if t.Gems == nil {
						t.Gems = make(map[string]Gem)
					}
					t.Gems[key] = v
					return true, nil
				}
			}
		}
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *World) Diff(other *World) deep.Patch[World] {
	p := deep.Patch[World]{}
	if t.Tick != other.Tick {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/tick", Old: t.Tick, New: other.Tick})
	}
	if t.Width != other.Width {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/width", Old: t.Width, New: other.Width})
	}
	if t.Height != other.Height {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/height", Old: t.Height, New: other.Height})
	}
	{
		_mapFrom := len(p.Operations)
		if (t.Players == nil) != (other.Players == nil) {
			p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/players", Old: t.Players, New: other.Players})
		} else {
			if other.Players != nil {
				for k, v := range other.Players {
					if t.Players == nil {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/players/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
						continue
					}
					oldV, ok := t.Players[k]
					switch {
					case !ok:
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpAdd, Path: "/players/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
					case !oldV.Equal(&v):
						sub := oldV.Diff(&v)
						prefix := "/players/" + deep.EscapePathKey(fmt.Sprintf("%v", k))
						for _, op := range sub.Operations {
							if op.Path == "" || op.Path == "/" {
								op.Path = prefix
							} else {
								op.Path = prefix + op.Path
							}
							p.Operations = append(p.Operations, op)
						}
					}
				}
			}
			if t.Players != nil {
				for k, v := range t.Players {
					if _, ok := other.Players[k]; !ok {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpRemove, Path: "/players/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), Old: v})
					}
				}
			}
		}
		gen.SortOperations(p.Operations[_mapFrom:])
	}
	{
		_mapFrom := len(p.Operations)
		if (t.Gems == nil) != (other.Gems == nil) {
			p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/gems", Old: t.Gems, New: other.Gems})
		} else {
			if other.Gems != nil {
				for k, v := range other.Gems {
					if t.Gems == nil {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/gems/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
						continue
					}
					oldV, ok := t.Gems[k]
					switch {
					case !ok:
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpAdd, Path: "/gems/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), New: v})
					case !oldV.Equal(&v):
						sub := oldV.Diff(&v)
						prefix := "/gems/" + deep.EscapePathKey(fmt.Sprintf("%v", k))
						for _, op := range sub.Operations {
							if op.Path == "" || op.Path == "/" {
								op.Path = prefix
							} else {
								op.Path = prefix + op.Path
							}
							p.Operations = append(p.Operations, op)
						}
					}
				}
			}
			if t.Gems != nil {
				for k, v := range t.Gems {
					if _, ok := other.Gems[k]; !ok {
						p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpRemove, Path: "/gems/" + deep.EscapePathKey(fmt.Sprintf("%v", k)), Old: v})
					}
				}
			}
		}
		gen.SortOperations(p.Operations[_mapFrom:])
	}

	return p
}

func (t *World) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/tick", "/Tick":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Tick, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Tick))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int64:
			_cv = float64(v)
		case float64:
			_cv = v
		case int:
			_cv = float64(v)
		default:
			return false, fmt.Errorf("condition value type mismatch for field Tick")
		}
		_fv := float64(t.Tick)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int64:
				for _, v := range vals {
					if t.Tick == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int64:
						if t.Tick == iv {
							return true, nil
						}
					case float64:
						if float64(t.Tick) == iv {
							return true, nil
						}
					case int:
						if float64(t.Tick) == float64(iv) {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/width", "/Width":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Width, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Width))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Width")
		}
		_fv := float64(t.Width)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Width == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Width == iv {
							return true, nil
						}
					case float64:
						if float64(t.Width) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/height", "/Height":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Height, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Height))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Height")
		}
		_fv := float64(t.Height)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Height == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Height == iv {
							return true, nil
						}
					case float64:
						if float64(t.Height) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *World) Equal(other *World) bool {
	if t.Tick != other.Tick {
		return false
	}
	if t.Width != other.Width {
		return false
	}
	if t.Height != other.Height {
		return false
	}
	if len(t.Players) != len(other.Players) {
		return false
	}
	for k, v := range t.Players {
		vOther, ok := other.Players[k]
		if !ok {
			return false
		}
		if !v.Equal(&vOther) {
			return false
		}
	}
	if len(t.Gems) != len(other.Gems) {
		return false
	}
	for k, v := range t.Gems {
		vOther, ok := other.Gems[k]
		if !ok {
			return false
		}
		if !v.Equal(&vOther) {
			return false
		}
	}
	return true
}

// Clone returns a deep copy of t.
func (t *World) Clone() *World {
	res := &World{
		Tick:   t.Tick,
		Width:  t.Width,
		Height: t.Height,
	}
	if t.Players != nil {
		res.Players = make(map[string]Player)
		for k, v := range t.Players {
			res.Players[k] = *v.Clone()
		}
	}
	if t.Gems != nil {
		res.Gems = make(map[string]Gem)
		for k, v := range t.Gems {
			res.Gems[k] = *v.Clone()
		}
	}
	return res
}

// Patch applies p to t using the generated fast path.
func (t *Player) Patch(p deep.Patch[Player], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Player) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Player); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Player](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/x", "/X":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.X == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.X, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.X)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.X = v
				return true, nil
			}
		}
	case "/y", "/Y":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Y == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Y, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Y)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Y = v
				return true, nil
			}
		}
	case "/score", "/Score":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Score == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Score, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Score)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Score = v
				return true, nil
			}
		}
	case "/hue", "/Hue":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Hue == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Hue, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Hue)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Hue = v
				return true, nil
			}
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Player) Diff(other *Player) deep.Patch[Player] {
	p := deep.Patch[Player]{}
	if t.X != other.X {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/x", Old: t.X, New: other.X})
	}
	if t.Y != other.Y {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/y", Old: t.Y, New: other.Y})
	}
	if t.Score != other.Score {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/score", Old: t.Score, New: other.Score})
	}
	if t.Hue != other.Hue {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/hue", Old: t.Hue, New: other.Hue})
	}

	return p
}

func (t *Player) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/x", "/X":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.X, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.X))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field X")
		}
		_fv := float64(t.X)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.X == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.X == iv {
							return true, nil
						}
					case float64:
						if float64(t.X) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/y", "/Y":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Y, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Y))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Y")
		}
		_fv := float64(t.Y)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Y == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Y == iv {
							return true, nil
						}
					case float64:
						if float64(t.Y) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/score", "/Score":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Score, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Score))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Score")
		}
		_fv := float64(t.Score)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Score == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Score == iv {
							return true, nil
						}
					case float64:
						if float64(t.Score) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/hue", "/Hue":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Hue, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Hue))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Hue")
		}
		_fv := float64(t.Hue)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Hue == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Hue == iv {
							return true, nil
						}
					case float64:
						if float64(t.Hue) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Player) Equal(other *Player) bool {
	if t.X != other.X {
		return false
	}
	if t.Y != other.Y {
		return false
	}
	if t.Score != other.Score {
		return false
	}
	if t.Hue != other.Hue {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Player) Clone() *Player {
	res := &Player{
		X:     t.X,
		Y:     t.Y,
		Score: t.Score,
		Hue:   t.Hue,
	}
	return res
}

// Patch applies p to t using the generated fast path.
func (t *Gem) Patch(p deep.Patch[Gem], logger *slog.Logger) error {
	if logger == nil {
		logger = slog.Default()
	}
	if p.Guard != nil {
		ok, err := t.evaluateCondition(*p.Guard)
		if err != nil {
			return fmt.Errorf("global condition evaluation failed: %w", err)
		}
		if !ok {
			return fmt.Errorf("global condition not met")
		}
	}
	var errs []error
	for _, op := range p.Operations {
		op.Strict = p.Strict
		handled, err := t.applyOperation(op, logger)
		if err != nil {
			errs = append(errs, err)
		} else if !handled {
			if err := gen.ApplyOpReflection(t, op, logger); err != nil {
				errs = append(errs, err)
			}
		}
	}
	if len(errs) > 0 {
		return &deep.ApplyError{Errors: errs}
	}
	return nil
}

func (t *Gem) applyOperation(op deep.Operation, logger *slog.Logger) (bool, error) {
	if op.If != nil {
		ok, err := t.evaluateCondition(*op.If)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if !ok {
			return true, nil
		}
	}
	if op.Unless != nil {
		ok, err := t.evaluateCondition(*op.Unless)
		if err != nil {
			return true, fmt.Errorf("condition evaluation failed at %s: %w", op.Path, err)
		}
		if ok {
			return true, nil
		}
	}
	if op.Kind == deep.OpLog {
		logger.Info("deep log", "message", op.New, "path", op.Path)
		return true, nil
	}

	switch op.Path {
	case "/":
		if op.Strict && op.Old != nil && (op.Kind == deep.OpReplace || op.Kind == deep.OpRemove) {
			_match := false
			if old, ok := op.Old.(Gem); ok {
				_match = deep.Equal(*t, old)
			}
			if !_match {
				_match = deep.EqualCoerced(*t, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at root: expected %v, got %v", op.Old, *t)
			}
		}
		if op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[Gem](op.New); ok {
				*t = v
				return true, nil
			}
		}
		return true, fmt.Errorf("unsupported root operation: %s", op.Kind)
	case "/x", "/X":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.X == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.X, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.X)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.X = v
				return true, nil
			}
		}
	case "/y", "/Y":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Y == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Y, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Y)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Y = v
				return true, nil
			}
		}
	case "/value", "/Value":
		if op.Kind == deep.OpReplace && op.Strict && op.Old != nil {
			_match := false
			if _oldV, ok := op.Old.(int); ok {
				_match = t.Value == _oldV
			}
			if !_match {
				_match = deep.EqualCoerced(t.Value, op.Old)
			}
			if !_match {
				return true, fmt.Errorf("strict check failed at %s: expected %v, got %v", op.Path, op.Old, t.Value)
			}
		}
		if op.Kind == deep.OpAdd || op.Kind == deep.OpReplace {
			if v, ok := deep.ValueAs[int](op.New); ok {
				t.Value = v
				return true, nil
			}
		}
	default:
	}
	return false, nil
}

// Diff compares t with other and returns a Patch.
func (t *Gem) Diff(other *Gem) deep.Patch[Gem] {
	p := deep.Patch[Gem]{}
	if t.X != other.X {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/x", Old: t.X, New: other.X})
	}
	if t.Y != other.Y {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/y", Old: t.Y, New: other.Y})
	}
	if t.Value != other.Value {
		p.Operations = append(p.Operations, deep.Operation{Kind: deep.OpReplace, Path: "/value", Old: t.Value, New: other.Value})
	}

	return p
}

func (t *Gem) evaluateCondition(c condition.Condition) (bool, error) {
	switch c.Op {
	case "and":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err != nil || !ok {
				return false, err
			}
		}
		return true, nil
	case "or":
		for _, sub := range c.Sub {
			ok, err := t.evaluateCondition(*sub)
			if err == nil && ok {
				return true, nil
			}
		}
		return false, nil
	case "not":
		if len(c.Sub) > 0 {
			ok, err := t.evaluateCondition(*c.Sub[0])
			if err != nil {
				return false, err
			}
			return !ok, nil
		}
		return true, nil
	}

	switch c.Path {
	case "/x", "/X":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.X, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.X))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field X")
		}
		_fv := float64(t.X)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.X == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.X == iv {
							return true, nil
						}
					case float64:
						if float64(t.X) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/y", "/Y":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Y, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Y))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Y")
		}
		_fv := float64(t.Y)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Y == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Y == iv {
							return true, nil
						}
					case float64:
						if float64(t.Y) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	case "/value", "/Value":
		if c.Op == "exists" {
			return true, nil
		}
		if c.Op == "type" {
			tn, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("type requires string value")
			}
			return condition.CheckType(t.Value, tn), nil
		}
		if c.Op == "matches" {
			pat, ok := c.Value.(string)
			if !ok {
				return false, fmt.Errorf("matches requires string pattern")
			}
			return regexp.MatchString(pat, fmt.Sprintf("%v", t.Value))
		}
		var _cv float64
		switch v := c.Value.(type) {
		case int:
			_cv = float64(v)
		case float64:
			_cv = v
		default:
			return false, fmt.Errorf("condition value type mismatch for field Value")
		}
		_fv := float64(t.Value)
		switch c.Op {
		case "==":
			return _fv == _cv, nil
		case "!=":
			return _fv != _cv, nil
		case ">":
			return _fv > _cv, nil
		case "<":
			return _fv < _cv, nil
		case ">=":
			return _fv >= _cv, nil
		case "<=":
			return _fv <= _cv, nil
		case "in":
			switch vals := c.Value.(type) {
			case []int:
				for _, v := range vals {
					if t.Value == v {
						return true, nil
					}
				}
			case []any:
				for _, v := range vals {
					switch iv := v.(type) {
					case int:
						if t.Value == iv {
							return true, nil
						}
					case float64:
						if float64(t.Value) == iv {
							return true, nil
						}
					}
				}
			}
			return false, nil
		}
	}
	// Anything the fast path does not model — nested paths, collection
	// lookups, unusual ops — is evaluated by the reflection engine, which
	// handles the full condition language.
	return condition.Evaluate(reflect.ValueOf(t).Elem(), &c)
}

// Equal returns true if t and other are deeply equal.
func (t *Gem) Equal(other *Gem) bool {
	if t.X != other.X {
		return false
	}
	if t.Y != other.Y {
		return false
	}
	if t.Value != other.Value {
		return false
	}
	return true
}

// Clone returns a deep copy of t.
func (t *Gem) Clone() *Gem {
	res := &Gem{
		X:     t.X,
		Y:     t.Y,
		Value: t.Value,
	}
	return res
}
